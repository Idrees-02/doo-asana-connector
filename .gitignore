# ---------------------------------------------------------------------------
# Secrets — this block is intentionally first. Nothing here is ever committed.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
.asana-credentials.json
.credentials/
secrets.json

# Dependencies
node_modules/
.npm/
.pnpm-store/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/
*.lcov

# Logs
logs/
*.log
npm-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~

# Vite / frontend
frontend/dist/
frontend/.vite/
.cache/

# Local scratch
tmp/
.tmp/
*.local

# Local editor/agent tooling
.claude/
IMG_8810.jpg

# Source 3D models. Nothing on the site renders one any more; this only keeps
# a stray export from being committed by accident.
*.glb

# Local Claude Code agent tooling. Removed from the repository entirely:
# unrelated to the connector, no source file referenced it, and several
# vendored skills shipped without a licence. Ignored so a stray copy cannot
# be committed back.
claude/

# Durable idempotency records (IDEMPOTENCY_STORE=file). Runtime state, and
# they contain user data (task names, comment text), so never committed.
.idempotency/
